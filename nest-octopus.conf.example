[octopus]
# Octopus Energy tariff code
# Example: E-1R-AGILE-FLEX-22-11-25-H
tariff_code = E-1R-AGILE-FLEX-22-11-25-H

[nest]
# Name of the thermostat device (matches the customName or partial device ID)
thermostat_name = Living Room Thermostat

# OAuth2 Client ID for Google Nest API
client_id = your-client-id.apps.googleusercontent.com

# Google Cloud Project ID
project_id = your-project-id

[heating]
# Temperature setpoints in Celsius
low_price_temp = 20.0
average_price_temp = 17.0

# Price classification thresholds (optional)
# low_price_threshold: multiplier for determining LOW prices (default: 75%)
#   LOW prices are below min(daily_avg, weekly_avg) * low_price_threshold
# high_price_threshold: multiplier for determining HIGH prices (default: 133%)
#   HIGH prices are above max(daily_avg, weekly_avg) * high_price_threshold
# low_price_threshold = 75%
# high_price_threshold = 133%
